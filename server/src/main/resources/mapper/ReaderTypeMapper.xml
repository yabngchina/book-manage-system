<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yabng.mapper.ReaderTypeMapper">
    <insert id="save" keyProperty="id" useGeneratedKeys="true">
        insert into reader_type(name, can_lend_quantity, can_lend_day, can_continue_times, punish_rate, date_valid)
        values(#{name}, #{canLendQuantity}, #{canLendDay}, #{canContinueTimes}, #{punishRate}, #{dateValid})
    </insert>

    <update id="update">
        update reader_type
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="canLendQuantity != null">can_lend_quantity = #{canLendQuantity},</if>
            <if test="canLendDay != null">can_lend_day = #{canLendDay},</if>
            <if test="canContinueTimes != null">can_continue_times = #{canContinueTimes},</if>
            <if test="punishRate != null">punish_rate = #{punishRate},</if>
            <if test="dateValid != null">date_valid = #{dateValid},</if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from reader_type where id = #{id}
    </delete>

    <select id="selectAll" resultType="com.yabng.domain.pojo.ReaderType">
        select * from reader_type
    </select>
    <select id="getById" resultType="com.yabng.domain.pojo.ReaderType">
        select * from reader_type where id = #{id}
    </select>
    <select id="selectByIds" resultType="com.yabng.domain.pojo.ReaderType">
        select * from reader_type where id in
        <foreach item="id" collection="ids" separator="," open="(" close=")">
            #{id}
        </foreach>
    </select>
</mapper>